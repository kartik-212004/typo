/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./**/*.html'], // This pattern includes all HTML files in the current directory and its subdirectories
  theme: {
    extend: {},
  },
  plugins: [],
}
